[package]
name = "verdantd"
version = "0.1.0"
edition = "2024"

[dependencies]
bloom = { path = "../bloom" }
nix = { version = "0.30.1", features = ["signal", "user"] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
